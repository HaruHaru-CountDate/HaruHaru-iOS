import SwiftUI

struct MainView: View {
    var body: some View {
        VStack {
            Text("zzz")
                .foregroundStyle(.white)
                .frame(width: 300, height: 300)
        }
        .background(.black)
    }
}

#Preview {
    MainView()
}
